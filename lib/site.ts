/**
 * Build-time site configuration. Editable business details (name, email,
 * social links, etc.) live in Payload → Settings → Site settings.
 */
export const site = {
  url: "https://opal-life-coaching.vercel.app",
  /** Flip to true when ready for search engines. */
  allowIndexing: false,
} as const;
